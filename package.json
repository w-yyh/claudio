{
  "name": "claudio",
  "version": "0.1.0",
  "description": "AI DJ powered by Claude — personal radio with context-aware music selection",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "desktop": "electron client/main.cjs",
    "test": "node --import ./tests/helpers/setup-env.js --test tests/**/*.test.js",
    "test:adapters": "node scripts/test-adapters.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.4.0",
    "express": "^4.18.0",
    "node-fetch": "^3.3.0",
    "node-ssdp": "^4.0.1",
    "ws": "^8.17.0",
    "xml2js": "^0.6.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "electron": "^41.4.0"
  }
}
